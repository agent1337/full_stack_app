import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    input: {
        marginTop: '15px'
    },
    form: {
        display: 'flex',
        flexDirection: 'column'
    }
});
