import {makeStyles} from '@material-ui/core/styles'

export const useStyles = makeStyles({
    input: {
        margin: '15px 0'
    },
    form: {
        display: 'flex',
        flexDirection: 'column'
    }
})
